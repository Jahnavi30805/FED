import {useSelector,useDispatch} from 'react-redux';import {add} from './feedbackSlice';import {useState} from 'react';function App(){const fb=useSelector(s=>s.feedback);const dispatch=useDispatch();const[r,setR]=useState(''),[c,setC]=useState('');return(<div><h2>Feedback</h2><select onChange={e=>setR(e.target.value)}><option>Rate</option>{[1,2,3,4,5].map(n=><option key={n}>{n}</option>)}</select><input placeholder='Comment' onChange={e=>setC(e.target.value)}/><button onClick={()=>dispatch(add({r,c}))}>Submit</button>{fb.map((f,i)=><p key={i}>{f.r}⭐ {f.c}</p>)}</div>);}export default App;